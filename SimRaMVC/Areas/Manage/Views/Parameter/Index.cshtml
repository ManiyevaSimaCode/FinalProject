@model List<ParameterGetDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Manage/Views/Shared/_Layout.cshtml";
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h2 class="card-title">Product Parameters</h2>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                Parameter Name
                            </th>
                            <th>
                                Created Date
                            </th>
                            <th>
                                Operations
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach(ParameterGetDto parameterGetDto in Model)
                        {
                             <tr>
                            <td>
                               @parameterGetDto.Name
                            </td>
                            <td>
                                @parameterGetDto.CreatedDate
                            </td>

                            <td>
                                <a type="button" asp-action="Update" asp-controller="Parameter" asp-area="Manage" asp-route-Id=@parameterGetDto.Id  class="btn btn-primary btn-icon-text">
                                    <i class="ti-pencil btn-icon-prepend"></i>

                                </a>

                                <a type="button" asp-action="Remove" asp-controller="Parameter" asp-area="Manage" asp-route-Id=@parameterGetDto.Id   class="btn btn-danger btn-icon-text sweet-delete">
                                    <i class="ti-trash btn-icon-prepend"></i>
                                </a>
                            </td>

                        </tr>

                        }
                       

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
